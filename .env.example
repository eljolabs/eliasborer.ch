# Copy values into your shell or Compose environment; no secrets belong here.
SITE_URL=https://eliasborer.ch
BASE_PATH=/
# Local preview servers bind to loopback by default.
DEV_BIND_IP=127.0.0.1
HOST_BIND_IP=127.0.0.1
